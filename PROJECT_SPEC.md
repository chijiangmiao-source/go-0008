# 谷物熏蒸监测与通风安全解锁服务

## 项目目标

建设一个无前端的标准规模 Go 后端，以事件时间、版本化安全规范和可恢复状态机管理谷物熏蒸。系统从作业登记开始，持续接收密闭仓或集装箱内多探头读数，形成可审计的有效作用时长证据；异常时进入偏差处置，达标后依次控制分阶段通风、人员准入解锁和不可变归档。核心范围严格限定为六个指定组件，并通过持久化事件、幂等控制命令和确定性传感器回放保证并发、重启及故障条件下的安全一致性。生产代码目标约 2500 行，控制在 2000—3000 行且不少于 20 个生产 Go 文件。

## 端到端业务流程

1. 作业登记：操作人员创建熏蒸作业，填写场所、密闭载体、药剂、剂量、密封时间、粮堆区域及探头覆盖关系；服务校验数据并绑定当时可用的安全规范版本，生成处于“已登记”状态的作业。
2. 密封与监测：作业确认密封后进入作用阶段，传感器持续并发上报浓度、温度和压力。接收端完成单位标准化、事件校时、去重、乱序整理、采样缺口识别和异常探头隔离，再将有效证据交给计算器。
3. 作用量判定与偏差处置：计算器按区域覆盖、规范版本和有效时间片累计作用时长；浓度越界、探头失联、时钟异常或覆盖不足时暂停累计并创建偏差，只有完成记录化处置且重新满足监测条件后才能恢复。
4. 通风与人员准入：作用条件满足后，联锁控制器按固定顺序执行各通风阶段，同一作业只允许一个阶段活动。阶段反馈、环境读数和超时条件均合格后方可推进，最终通过人员进入条件检查并提交一次不可逆解锁。
5. 中止、恢复与归档：紧急中止立即停止阶段推进并保持准入锁定，显式复位需重新验证传感器和控制器状态。完成解锁的作业汇总规范快照、读数、计算片段、偏差和控制回执，生成不可变归档；重启时从事件与检查点恢复相同结果。

## 核心组件与职责

1. 作业登记 API：负责作业创建、密封确认、状态查询及归档请求，校验药剂、剂量、区域和探头覆盖配置，并以版本号保护并发状态变更。
2. 传感器事件接收与校时器：接收批量或单条读数，以事件标识和探头序列号保证幂等，完成单位换算、时间校正、乱序缓冲、缺口判定及探头健康评估。
3. 暴露量计算器：把校正后的读数切分为确定性时间片，依据区域覆盖与生效规范计算累计暴露量和有效作用时长，并为每个计入或排除决定保存原因。
4. 偏差处置状态机：管理越界、失联、采样缺口、时钟回拨和控制异常，记录发现、确认、措施、复核及关闭过程，并控制作业暂停和安全恢复。
5. 通风联锁控制器：顺序执行通风阶段，处理阶段互斥、控制回执、超时、紧急中止、复位和人员准入，守住物理解锁的不可逆提交边界。
6. 持久化及事件恢复模块：事务化保存聚合状态、事件、控制命令和检查点，通过幂等发件箱投递外部命令，并在重启后重放未完成作业和构建完整归档。

## 领域规则与不变量

1. 主状态仅允许“已登记→已密封→作用中→待通风→通风中→准入已解锁→已归档”；非法跳转必须拒绝并保留审计记录。
2. 准入解锁前的作业可因监测或控制异常转入“处置中”或“紧急中止”，恢复时必须由显式处置或复位命令返回记录的安全前态。
3. 读数使用校正后的事件时间参与计算；接收时间仅用于审计、迟到窗口和运行监控，不得直接增加有效作用时长。
4. 同一作业、探头和事件标识只能产生一次业务影响；重复上报返回原处理结果，不得重复累计时间、创建偏差或推进状态。
5. 乱序读数可在配置的迟到窗口内重排并重算未封账片段；早于计算水位线的迟到数据只能隔离审计，不得静默改写已提交结论。
6. 每个登记区域必须达到既定健康探头覆盖；采样缺口或覆盖不足的时间片不计入有效时长，并在超过规范容忍期后创建偏差。
7. 浓度、温度和压力必须先转换为规范单位，再按公开规范样例分段计算；无法转换、数值非法或超出设备量程的读数不得进入计算。
8. 规范版本不可修改，依据校正事件时间的生效点选用；版本切换只影响生效点后的时间片，不追溯改写此前已封账的计算片段。
9. 异常探头可被隔离，但隔离不能掩盖区域覆盖不足；剩余探头不满足登记覆盖规则时，作业必须暂停累计并进入偏差处置。
10. 通风阶段严格顺序且互斥，只有前一阶段取得匹配的控制回执并满足监测条件后才能推进；超时或冲突命令保持锁定并进入处置。
11. 紧急中止立即冻结累计和阶段推进，取消未执行控制意图并保持人员锁定；复位必须显式发起、原因已消除且通过一轮新鲜读数验证。
12. 不可逆边界以物理解锁成功回执持久化为准；边界前可使用同一命令号安全重试，边界后不得通过回放撤销，新增风险应触发疏散事件而非伪造回锁。

## 数据模型与持久化

1. FumigationOperation：作业编号、机构与场所、载体类型、药剂、剂量、密封时间、区域配置、覆盖规则、当前状态、修订号和时间戳。
2. ProbeRegistration：探头编号、所属区域、支持量程、校准参数、启停时间、健康状态、隔离原因及最后有效事件信息。
3. SensorReading：事件编号、探头与序列号、设备时间、接收时间、校正时间、原始值与单位、标准化值、有效性及拒绝原因。
4. SafetyStandardVersion：版本号、来源引用、生效时间、暴露与残留规则、单位换算表、容忍窗口及不可变内容摘要。
5. ExposureLedger：作业水位线、累计有效时长与暴露量，以及包含起止时间、区域证据、规范版本、计入结果和理由的计算片段。
6. DeviationCase：偏差类型、严重度、触发证据、原安全状态、处置状态、措施、复核结果、责任记录及开启和关闭时间。
7. VentilationExecution：计划阶段、活动阶段、阶段期限、命令编号、控制器回执、互斥令牌、中止原因和复位记录。
8. EntryPermit：准入请求、条件快照、解锁命令、成功回执、不可逆提交时间，以及解锁后风险或疏散事件引用。
9. EventRecordAndArchive：递增事件序号、聚合版本、事件载荷、发件箱状态、检查点，以及归档清单、记录数量和完整性摘要。

## 公开接口

1. POST /v1/operations：登记熏蒸作业及区域探头配置，成功返回作业编号、初始状态、聚合修订号和绑定的规范信息。
2. POST /v1/operations/{id}/seal：确认密封并进入监测流程；要求携带预期修订号，状态冲突返回可判定的冲突响应。
3. POST /v1/operations/{id}/readings：批量接收传感器事件，逐条返回已接受、重复、隔离或拒绝结果，避免单条坏数据丢弃整批有效数据。
4. GET /v1/operations/{id}：查询当前状态、有效作用时长、区域覆盖、探头健康、未关闭偏差、通风阶段及准入状态。
5. GET /v1/operations/{id}/evidence：分页查询标准化读数、缺口、计算片段、规范版本和状态变化组成的审计证据链。
6. POST /v1/operations/{id}/deviations/{deviationId}/resolve：提交处置措施和复核证据；仅在恢复条件满足时关闭偏差并恢复流程。
7. POST /v1/operations/{id}/ventilation/commands：请求开始或推进通风，使用幂等键和预期阶段防止重复控制及并发阶段冲突。
8. POST /v1/operations/{id}/emergency-stop 与 /reset：分别执行紧急中止和受控复位，响应中明确锁定状态、待验证条件及执行结果。
9. POST /v1/operations/{id}/entry-permit：请求人员准入，服务在同一决策中检查阶段回执、最新读数、残留限制和未关闭偏差。
10. POST /v1/operations/{id}/archive：在终态生成归档清单和完整性摘要；未满足准入提交或证据完整性要求时拒绝归档。
11. VentilationControllerPort：以作业号、阶段和唯一命令号发送控制指令并接收确认；控制器和本地模拟器必须对重复命令返回一致结果。
12. EventStorePort：提供聚合版本追加、检查点读取、待投递命令扫描和归档快照能力，支持故障注入与确定性重放测试。

## 失败边界

1. 输入边界：缺字段、未知单位、非法数值或未登记探头只影响对应事件，并返回稳定错误码；这些事件不能改变状态或暴露累计。
2. 时间边界：无法校时、设备时钟回拨或超出迟到窗口的读数进入隔离区；校时器失败不得回退到接收时间继续计算。
3. 覆盖边界：单探头异常先隔离，随后重新评估区域覆盖；覆盖不充分时暂停该时间片并创建偏差，禁止用其他区域数据代替。
4. 规范边界：找不到适用版本、换算定义缺失或规范内容校验失败时停止相关计算，保留原始证据并阻止通风和准入推进。
5. 并发边界：状态和通风阶段以聚合修订号及互斥令牌提交；竞争请求仅一个成功，其余返回冲突且不得产生外部副作用。
6. 控制边界：先持久化控制意图再投递，超时或未知回执保持锁定；回执落库失败时仅使用原命令号重试，避免设备重复执行。
7. 恢复边界：重启后从检查点和后续事件重建状态，已确认命令不得重发，未确认命令可幂等续投，重放结果必须与中断前一致。
8. 归档边界：快照、清单或完整性摘要任一步失败均保留可重试状态，不得提前标记已归档或删除在线证据。

## 验收标准

1. 交付可配置、可编译、可运行且不含前端的 Go HTTP 后端；执行统一测试命令可完成单元、集成和竞态验证，并提供本地传感器及控制器回放方式。
2. 生产代码不少于 2000 行，目标约 2500 行并控制在 2000—3000 行附近；至少包含 20 个生产 Go 文件，规模来自六个真实组件、并发控制和持久化实现。
3. 所有主状态、处置状态和非法跳转均可通过 API 观察和测试，重复或竞争请求不会造成重复累计、并行通风阶段或多次物理解锁。
4. 使用公开粮食熏蒸规范制作的脱敏阈值、单位换算样例和固定时钟，能够确定性验证有效作用时长、缺口、探头隔离及版本生效点。
5. 通风阶段互斥、超时、中止、复位和准入判定均默认保持安全锁定；不可逆提交前后的重试及重放行为具有明确且可验证的差异。
6. 任意流程节点重启后，恢复出的状态、累计值、偏差、待执行命令和控制结果与不中断运行一致，且不会重复产生外部控制副作用。
7. 归档包含配置、规范快照、原始与校正读数、计算片段、偏差、命令回执、准入记录和事件摘要，并能检测缺项或内容篡改。

## 确定性测试场景

1. 完成从登记、密封、有效监测、作用达标、分阶段通风、准入解锁到归档的完整正常流程，并核对每次状态迁移。
2. 对每个状态提交非法跳转，并并发发送使用同一修订号的合法命令，验证只有一个提交成功且审计记录完整。
3. 用固定浓度、温度和压力序列及多种合法单位验证分段换算、暴露量和有效作用时长，覆盖边界值与舍入规则。
4. 多个探头并发发送相同事件、相同序列号及重复批次，验证接收结果稳定且累计、偏差和事件记录均只出现一次。
5. 在迟到窗口内逆序发送读数并跨水位线发送更旧读数，验证前者重排重算、后者隔离且不改写封账结果。
6. 制造恰好等于和超过容忍期的采样缺口，验证有效时长暂停、偏差触发时点及补发数据对封账片段的影响。
7. 模拟设备时钟逐步漂移和突然回拨，验证校时、隔离及恢复过程不会使用接收时间虚增作用时长。
8. 让单个探头越界或失联，再分别保留和破坏区域覆盖，验证异常探头隔离、剩余证据计算及覆盖不足处置。
9. 让作业跨越规范版本生效点，验证时间片按校正事件时间分段选版，旧片段不被追溯修改且归档保留两版快照。
10. 并发启动两个通风阶段并注入乱序控制回执，验证阶段顺序、互斥令牌、回执匹配和重复命令幂等性。
11. 令活动通风阶段超过期限或返回失败，验证流程进入处置、人员保持锁定，并在完成措施后从安全阶段恢复。
12. 在作用中和通风中分别触发紧急中止，验证计时及命令冻结；原因未消除时拒绝复位，新鲜读数验证后才允许继续。
13. 在解锁发送前、设备执行后但回执落库前以及回执已提交后三个位置模拟故障，验证不可逆边界和重试策略。
14. 在各关键事件后重启服务并执行归档，验证事件重放一致性、未确认命令续投、已确认命令抑制及归档完整性检查。

## 组件追踪关系

1. 作业登记 API 对应登记和归档流程，落地 FumigationOperation 与规范绑定规则，通过创建、密封、查询和归档接口验证状态合法性。
2. 传感器事件接收与校时器对应持续监测流程，落地 ProbeRegistration 与 SensorReading，覆盖并发、重复、乱序、缺口、回拨和隔离测试。
3. 暴露量计算器对应作用量判定流程，维护 ExposureLedger，落实单位换算、区域覆盖和规范生效规则，由固定回放序列验证确定性。
4. 偏差处置状态机贯穿监测及恢复流程，维护 DeviationCase，承接越界、失联和覆盖不足，并通过处置接口与中止复位场景验证。
5. 通风联锁控制器对应通风和准入流程，维护 VentilationExecution 与 EntryPermit，覆盖阶段互斥、超时、幂等控制及不可逆解锁边界。
6. 持久化及事件恢复模块支撑全部流程，维护事件、发件箱、检查点和归档清单，通过故障注入、逐节点重启及完整性测试验证。

## 独特性

本项目不是通用物联网看板，而是以“可证明的作用时长”和“不可逆的人员准入解锁”为双核心：事件时间、区域覆盖和规范版本共同决定每个时间片是否有效，外部通风命令则通过事件恢复与幂等回执守住物理安全边界。重复、乱序、缺口、回拨、版本切换、探头隔离、阶段冲突、控制超时、中止复位、解锁提交及重启归档形成彼此独立的故障注入与缺陷验证空间。
